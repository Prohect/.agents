{{!-- Commented sections below document this tool's fixed API contract — already
sent with the tool's schema and stripped from the prompt. Keep the guidance
consistent with them. --}}
{{!--
Find file paths that match a given pattern.
--}}

{{!--
- Supports glob patterns like "**/*.js" or "src/**/*.ts"
- Returns matching file paths sorted alphabetically
--}}
- Prefer the `grep` tool to this tool when searching for symbols unless you have specific information about paths.
- Use this tool when you need to find files by name patterns
- Limited to project roots; respects `gitignore`, `file_scan_exclusions` & `file_scan_inclusions`.
{{!--
- Results are paginated with 50 matches per page. Use the optional 'offset' parameter to request subsequent pages.
--}}
